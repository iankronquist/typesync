TypeSync
========

A TypeScript implementation of TimeSync using the Express framework.

Quick Start
-----------
Requires make, npm, and nodejs.

```
$ npm install
$ make configure
$ make build
$ make run
```
